/**
 * 小程序配置文件
 */
//接口域名
//var host = "http://127.0.0.1:4455";
var host = "https://sing.dazonghetong.com";

var config = {
    host,
    baseUrl: `${host}`,
    imgUrl: `${host+"/f/"}`
};

module.exports = config
